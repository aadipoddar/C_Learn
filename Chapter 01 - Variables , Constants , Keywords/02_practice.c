#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The are of the circle id %f\n", pi * radius * radius);

    int height = 3;
    printf("Volume of the cylinder is %f\n", pi * radius * radius * height);
    return 0;
}